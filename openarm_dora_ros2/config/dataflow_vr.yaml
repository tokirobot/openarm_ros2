nodes:
  - id: ui
    build: pip install git+https://github.com/enactic/dora-openarm-data-collection-ui
    path: dora-openarm-data-collection-ui
    env:
      METADATA_FILE: "test_metadata.yaml"
    inputs:
      tick: dora/timer/secs/1
      button_a:
        source: udp-receiver/button_a
        queue_size: 100
      button_b:
        source: udp-receiver/button_b
        queue_size: 100
    outputs:
      - command

  - id: quittable-tick-leader
    build: pip install git+https://github.com/enactic/dora-openarm-quitter
    path: dora-openarm-quitter
    inputs:
      command: ui/command
      tick: dora/timer/millis/2
    outputs:
      - tick

  - id: quittable-tick-camera
    build: pip install git+https://github.com/enactic/dora-openarm-quitter
    path: dora-openarm-quitter
    inputs:
      command: ui/command
      tick: dora/timer/millis/33
    outputs:
      - tick

  - id: udp-receiver
    build: pip install -e ..
    path: dora-quest-receiver
    args: "--host 0.0.0.0 --port 5006"
    inputs:
      tick: quittable-tick-leader/tick
    outputs:
      - pose_right
      - trigger_right
      - pose_left
      - trigger_left
      - joystick_y
      - button_a
      - button_b
      - status

  - id: ik
    build: pip install git+ssh://git@github.com/reazon-research/dora-openarm-kinematics.git
    path: dora-openarm-ik
    args: "--mode bimanual --max-iters 10 --dt 0.1 --damping 0.1 --posture-cost 0.01 --lm-damping 0.01"
    inputs:
      tick: quittable-tick-leader/tick
      target_right: udp-receiver/pose_right
      target_left:  udp-receiver/pose_left
      trigger_right: udp-receiver/trigger_right
      trigger_left:  udp-receiver/trigger_left
    outputs:
      - position_left
      - position_right
      - status

  - id: dummy-camera
    path: ../node/dummy-camera.py
    outputs:
      - image

  # ── MuJoCo viewer  ──
  - id: mujoco-viewer
    build: pip install git+ssh://git@github.com/reazon-research/dora-openarm-mujoco.git
    path: dora-openarm-mujoco
    args: "--keyframe home --viewer --debug-frames "
    inputs:
      position_left:  ik/position_left
      position_right: ik/position_right
      pose_right:     udp-receiver/pose_right
      pose_left:      udp-receiver/pose_left
      joystick_y:     udp-receiver/joystick_y
    outputs:
      - status

  - id: camera-wrist-right
    build: pip install opencv-video-capture
    path: opencv-video-capture
    env:
      CAPTURE_PATH: "/dev/camera_wrist_right"
      ENCODING: "jpeg"
      IMAGE_HEIGHT: 600
      IMAGE_WIDTH: 960
      JPEG_QUALITY: 90
    inputs:
      tick: quittable-tick-camera/tick
    outputs:
      - image

  - id: camera-wrist-left
    build: pip install opencv-video-capture
    path: opencv-video-capture
    env:
      CAPTURE_PATH: "/dev/camera_wrist_left"
      ENCODING: "jpeg"
      IMAGE_HEIGHT: 600
      IMAGE_WIDTH: 960
      JPEG_QUALITY: 90
    inputs:
      tick: quittable-tick-camera/tick
    outputs:
      - image

  - id: camera-head-stereo
    build: pip install opencv-video-capture
    path: opencv-video-capture
    env:
      CAPTURE_PATH: "/dev/camera_head_stereo"
      ENCODING: "bgr8"
      IMAGE_HEIGHT: 720
      IMAGE_WIDTH: 2560
    inputs:
      tick: quittable-tick-camera/tick
    outputs:
      - image

  - id: camera-head-stereo-splitter
    build: pip install git+https://github.com/enactic/dora-opencv-image-splitter
    path: dora-opencv-image-splitter
    inputs:
      tick: quittable-tick-camera/tick
      image: camera-head-stereo/image
    env:
      SPLIT_MODE: "vertical"
      NUM_SPLITS: 2
      JPEG_QUALITY: 90
      IMAGE_HEIGHT: 720
      IMAGE_WIDTH: 2560
    outputs:
      - image_0
      - image_1

  # - id: camera-ceiling
  #   build: pip install opencv-video-capture
  #   path: opencv-video-capture
  #   env:
  #     CAPTURE_PATH: "/dev/camera_ceiling"
  #     ENCODING: "jpeg"
  #     IMAGE_HEIGHT: 600
  #     IMAGE_WIDTH: 960
  #     JPEG_QUALITY: 90
  #   inputs:
  #     tick: quittable-tick-camera/tick
  #   outputs:
  #     - image

  - id: dora-to-ros2
    build: pip install -e ..
    path: openarm-dora-ros2
    inputs:
      left_position: ik/position_left
      right_position: ik/position_right
      camera_image: dummy-camera/image